# {{text:project_name}} — forge-game

This index is a generated view. Durable machine state lives under tracked `.forge-game/` paths. Active-run artifact bundles live under ignored `.forge-game/runtime/artifacts/`, and normalized source bundles live under ignored `.forge-game/runtime/source-sets/`. Only accepted human-readable projections live under tracked `docs/forge-game/artifacts/`.

Delivery hierarchy: product feature → vertical delivery slice → technical tasks. Architecture covers the full project with progressive module detail.

- GDD: `{{text:gdd_ref}}`
- Roadmap: `{{text:roadmap_ref}}`
- Architecture: `{{text:architecture_ref}}`
- NFR: `{{text:nfr_ref}}`
- Engineering rules: `{{text:engineering_rules_ref}}`
- Engineering rule catalog: `{{text:engineering_catalog_ref}}`
- Unreal Engine: `{{text:unreal_engine_version}}`
- Target platforms: {{text:target_platforms_text}}
