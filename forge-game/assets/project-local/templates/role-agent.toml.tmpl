name = "{{text:role_agent_name}}"
description = "{{text:role_short_description}}"
sandbox_mode = "{{text:sandbox_mode}}"
developer_instructions = """
Use the project-local ${{text:role_slug}} skill and stay inside its artifact contract.
Read canonical refs from the skill, report evidence, and do not perform another role's gate.
{{text:write_boundary}}
"""
