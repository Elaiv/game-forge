# {{text:module_name}} module guidance

- Scope work in this directory to the `{{text:module_name}}` module.
- Preserve the module boundary documented in `{{text:architecture_ref}}`.
- Enforce `{{text:engineering_rules_ref}}` and stop if its project-local catalog hash is stale.
- Use repository-root canonical commands and keep requirement, test, and evidence links current.
- Implement module internals only as required by the approved delivery slice; do not turn a module into a horizontal delivery milestone.
- Escalate cross-module contract changes to architecture review before implementation.
